import '../img/logo.png'

function Header {
    return (
        <header>
            <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        </header>
    )
}